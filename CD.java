import java.io.IOException;

public class CD {
    public static void main(String[] args) throws IOException {
        // Since we did not implement code generation and all semantics checks are in
        // the parser
        // we just call the parser from this file
        A2.main(args);
    }
}
